#!/usr/bin/env bash
# Wrapper so consumers can run: ./vendor/cursor-harness/runtime/night-shift status
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${ROOT}${PYTHONPATH:+:$PYTHONPATH}"
exec python3 "${ROOT}/night_shift.py" "$@"
